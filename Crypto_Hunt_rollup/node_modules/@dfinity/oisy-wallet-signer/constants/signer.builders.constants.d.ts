import type { SignerBuilderFn, SignerBuilderMethods } from '../types/signer-builders';
export declare const MAX_CONSENT_MESSAGE_ARG_SIZE_BYTES = 500;
export declare const SIGNER_BUILDERS: Record<SignerBuilderMethods, SignerBuilderFn>;
